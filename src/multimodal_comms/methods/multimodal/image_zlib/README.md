# image_zlib

Losslessly compresses already encoded image bytes with zlib and restores the exact bytes. It measures transport compression, not neural image reconstruction. Covered by multimodal codec contracts.
