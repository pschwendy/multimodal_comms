# mixed_packet

Serializes typed text, bytes, and structured fields into one modality-neutral packet and restores their types. Use it for mixed application payloads; learned latent image/text fusion is evaluated separately.
