# novelty

Keeps sentences whose normalized embedding is not too similar to earlier sentences. Stateful mode also remembers what each receiver saw. Requires `sentence-transformers`; experiment: compression and frontier sweeps.
