# identity

Transmits the selected message view unchanged. It is lossless and is the traffic/fidelity baseline. Implementation: `methods/text/compressors.py`. Experiment: HiddenBench identity conditions and every benchmark smoke fixture.
