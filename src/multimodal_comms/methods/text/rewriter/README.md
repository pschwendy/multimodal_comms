# rewriter

A local GRPO-trained language model rewrites the discussion to a hard character target. It is abstractive and may alter meaning. Use `harvest_rewriter_data` -> `train_rewriter_grpo` -> `run_rewriter_sweep.sh`.
