# learned

Scores sentence embeddings with a trained selector and keeps the requested fraction, optionally deduplicating. Train with `build_selector_dataset` -> `train_selector`; evaluate with `run_learned_sweep.sh`.
