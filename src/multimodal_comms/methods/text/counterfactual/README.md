# counterfactual

Keeps sentences predicted to matter under counterfactual removal. Build data with `harvest_counterfactual_importance`, train with `train_counterfactual_scorer`, then benchmark the saved joblib model.
