# window

Keeps messages from the most recent configurable number of rounds. It is lossy and stateless; it changes content selection, not the `full_history`/`delta` view policy. Experiment: `run_compression_sweep.sh`.
