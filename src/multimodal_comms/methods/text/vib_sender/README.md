# vib_sender

An abstractive sender trained with a variational-information-bottleneck-style reward. The historical soft codelength objective can collapse; retain hard output limits and inspect semantic fidelity. Pipeline: `harvest_vib_data` -> `train_vib_grpo`.
