# saliency

Requests token-gradient saliency from the representation server and keeps high-saliency pieces under a character budget. It fails safely to the original message if the server is unavailable. Experiment: proposed-method sweeps.
