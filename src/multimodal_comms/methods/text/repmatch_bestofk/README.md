# repmatch_bestofk

Generates fixed extractive candidates and chooses the one closest to the original representation; the identity view is not a candidate. Run `repserver` before a representation-match sweep.
