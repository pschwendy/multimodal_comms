# certspan

Greedily deletes spans only while a representation-distance certificate remains under epsilon. It needs `repserver` and fails safely when certification is unavailable. Experiment: proposed-method sweep.
