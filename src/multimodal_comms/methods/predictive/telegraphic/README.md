# telegraphic

Generates a short telegraphic candidate and accepts it only when its semantic certificate passes. It falls back to the original text on failure; evaluate with proposed-method and semantic-fidelity runs.
