# ratediff

Searches how many predictive corrections can be omitted while a representation certificate remains within epsilon. It is deliberately lossy and needs both the predictor and representation server.
