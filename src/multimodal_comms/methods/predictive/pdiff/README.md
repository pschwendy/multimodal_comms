# pdiff

Predicts the next message with a shared causal model and sends edit corrections. Keeping every correction is lossless only when encoder and decoder replay the same numerical path. Evaluate with semantic-fidelity programs.
