# superpose

Encodes messages as latent tensors, binds each slot with keyed transforms, sums them into one overlapping packet, then unbinds and decodes per receiver. Capacity is soft and crosstalk grows with load. Train/evaluate with superposition and security programs.
