# mwnot_autoencoder

Pools the complete hidden-state sequence into generator vectors with the MWNOT operator instead of sampling positions. Train with `training.programs.pretrain_mwnot_autoencoder`; evaluate through the same autoencoder and benchmark interfaces.
