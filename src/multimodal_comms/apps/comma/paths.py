"""Resource paths for the bundled COMMA application."""

from pathlib import Path

APP_ROOT = Path(__file__).resolve().parent
IMAGE_ROOT = APP_ROOT / "images"
SOUND_ROOT = APP_ROOT / "sounds"
CONFIG_ROOT = APP_ROOT / "config"
